models:
  - name: stg_jaffle_shop__customers
    description: one unique customer pew row
    columns:
      - name: customer_id
        description: primary key
        data_tests:
          - unique
          - not_null
  - name: stg_jaffle_shop__orders
    description: one unique order per row 
    columns: 
      - name: order_id
        description: primary key
        data_tests: 
          - unique
          - not_null
      - name: customer_id
        description: foreign key from customers
        data_tests: 
          - relationships:
              arguments:
                to: ref('stg_jaffle_shop__customers')
                field: customer_id
      - name: status 
        description: "{{ doc('order_status') }}"
        data_tests: 
          - accepted_values:
              arguments:
                values:
                  - placed
                  - shipped
                  - returned
                  - completed
                  - return_pending